// This file was auto-generated by Fern from our API Definition.

/**
 * @example
 *     {
 *         dataset_id: "dataset_id",
 *         patch: {},
 *         traceIds: ["traceIds"]
 *     }
 */
export interface UpdateTracesRequest {
    dataset_id: string;
    /** Patch to apply to one or more memberships. Nullable annotations preserve the same three states as [`PatchField`]: explicit JSON `null` clears while omission is a no-op. */
    patch: UpdateTracesRequest.Patch;
    traceIds: string[];
}

export namespace UpdateTracesRequest {
    /**
     * Patch to apply to one or more memberships. Nullable annotations preserve the same three states as [`PatchField`]: explicit JSON `null` clears while omission is a no-op.
     */
    export interface Patch {
        /** New cluster id, or `null` to drop the trace from any cluster. */
        clusterId?: (string | null) | undefined;
        /** Replace the membership note, or `null` to clear it. */
        note?: (string | null) | undefined;
        /** New split, or `null` to mark unassigned. */
        split?: (Patch.Split | null) | undefined;
        /** Health status of a trace as judged by the dataset owner. */
        status?: (Patch.Status | null) | undefined;
    }

    export namespace Patch {
        /** New split, or `null` to mark unassigned. */
        export const Split = {
            Train: "train",
            Validation: "validation",
            Test: "test",
        } as const;
        export type Split = (typeof Split)[keyof typeof Split];
        /** Health status of a trace as judged by the dataset owner. */
        export const Status = {
            Ok: "ok",
            Warn: "warn",
            Error: "error",
        } as const;
        export type Status = (typeof Status)[keyof typeof Status];
    }
}
